{
  "name": "backend",
  "version": "0.1.0",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.141.0",
    "@aws-sdk/client-rekognition": "^3.137.0",
    "@aws-sdk/client-s3": "^3.137.0"
  }
}
